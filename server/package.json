{
  "name": "merged-app-server",
  "version": "1.0.0",
  "description": "REST API for tasks using Node.js and Express (exp6, extended with CORS)",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2"
  }
}
